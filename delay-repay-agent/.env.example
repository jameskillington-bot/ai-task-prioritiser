# Claude API
ANTHROPIC_API_KEY=
# Realtime Trains next-generation API token (https://api-portal.rtt.io)
RTT_TOKEN=
# ...or a legacy api.rtt.io account instead of a token
# RTT_USERNAME=
# RTT_PASSWORD=
# Mailbox app password (Gmail: Google Account > Security > App passwords)
DELAY_REPAY_IMAP_PASSWORD=
# Bank details: typed into forms by the browser, never shown to the model
DELAY_REPAY_SORT_CODE=
DELAY_REPAY_ACCOUNT_NUMBER=
# Optional operator logins, by ATOC code, e.g. GWR:
# DELAY_REPAY_SECRET_GW_USERNAME=
# DELAY_REPAY_SECRET_GW_PASSWORD=
